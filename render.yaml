# Render deployment configuration for Movie_Finding_Made_Easy

services:
  - type: web
    name: Movie_Finding_Made_Easy
    runtime: docker
    region: oregon
    branch: main
    rootDir: .
    autoDeploy: true            # automatically deploy on each push
    envVars:
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
